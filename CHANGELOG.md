# Changelog

## 0.5.6

- public re-pin: usdaeco-toolchain v0.3.10, usdaeco-core v0.9.5,
  usdaeco-datacentre v0.4.8, usdaeco-ifc v0.2.2.
- Record resolved tag revisions, preserve requirement ranges and historical
  fixture pins, and regenerate the package and resource plugin metadata.
- Republish the pinned example with the documented build step; retain its
  fixed fixture receipts and refresh current dependency provenance.
- Preserve the crate, all fifteen editable layers and committed images byte
  for byte; fresh stock renders pass. Regenerate thirteen lobby version stamps
  and update the gate's release-version assertion.
- Verify 159 checks and 29 structure rules with no failures; 332 tests pass,
  five optional cases skip. One offline Nix attempt evaluates two package
  derivations, then stops on the pinned broken IfcOpenShell package; builds
  and online public resolution remain unproven.

## 0.5.5

- public names → github.com/criad-com; pin toolchain v0.3.8.
- Align the example manifest toolchain pin and lobby derivation version stamps;
  retain all published data-centre result and render bytes.
- Verify 159 checks and 29 structure rules with no failures; 332 tests pass,
  five optional cases skip. Record the single unresolved Nix attempt.

## 0.5.4

- Pin toolchain v0.3.6 and enforce portable archived source references (S29).
- Refresh example reference paths and manifest evidence while retaining the
  published crate and images byte for byte.
- Preserve the data-centre fixture receipts; refresh the separate lobby example
  to the current CLI derivation stamp.

## 0.5.3

- Re-pin to train aeco-0.7.0: core v0.9.2, toolchain v0.3.5, IFC v0.2.0 and data-centre v0.4.5; keep the core requirement range.
- Declare historical baseline and acceptance pins as fixtures.
- Republish the stale example for the new data-centre source receipt and release stamps; all three source USD layers and expected findings remain byte-identical. Refresh the lobby derivation stamps.
- Verify 158 checks, 0 failed, including 28 structure rules; 332 tests pass with five optional skips. The opt-in real-data gate is not run; one Nix source-resolution attempt is not proven.

## 0.5.2

- Publish the flattened, self-contained data-centre example, editable own layers and independently rendered stock USD proof; gate result freshness and all S01–S28 rules.
- Split camera derivation opinions into six text layers below the 2 MB cap without changing composition, coverage shells or either existing study render.
- Keep archived source wrappers independent of dependency checkout locations; fix the example receipt time for reproducible study layers and refresh the lobby tool stamp.
- Adopt MIT and document runtime dependency licences; pin toolchain v0.3.2 for MIT support while retaining the v0.3.1 publication contract.

## 0.5.1

- Render eleven occlusion-clipped CriticalDoors coverage shells in light teal; retain Privacy shells for inspection and hide nominal sectors wherever a study shell exists.
- Frame the look-through from the study view and separate door, frame and reader colours with bounded unlit rendering.
- Measure overlapping shell/sector plan-area unions and enforce foreground, saturated-white and image size limits; retain all 153 checks and add two metric rows.
- Preserve 11/11 door coverage, zero privacy hits and the nine-API, 71-property schema contract.
- Mark every generated Mesh guide, lobby body and scenario obstacle as tessellated without a tolerance claim; regenerate lobby fixtures from their writers.
- Require the core Python validator plugin to import and every selected rule to load; add a core example row with seeded E15 failures so validation cannot pass without executing.
- Pin toolchain v0.3.0 for the released namespace and unrestricted-applicability lint fixes; retain data-centre v0.4.1.

## 0.5.0

- Rebase on core 0.9; flatten schema resources and move all existing tests to testenv.
- Expose 23 legacy rules through the Python UsdValidation plugin.
- Add pinned data-centre import/studies, owned renders and common example evidence.
- Preserve the nine-API, 71-property contract; retain raw legacy namespace/applicability lint failures pending toolchain IMP4.


## 0.4.8

- Publish `usdaeco_cctv.contract` as the reference IFC camera reader using
  ifcopenshell and the standard library, with no USD import or registration.
- Share fact decoding, unit conversion, driver selection, tier precedence
  and preset parsing with the importer; retain promotion bookkeeping and
  sparse USD authoring in their existing layers.
- Test all four project-unit combinations, occurrence/type overrides,
  conflicts, last-preset removal and import with USD unavailable.
- Keep schema metadata 0.2.1 and study algorithm revision 4.

## 0.4.7

- Restore the demo Revit import budget with indexed property-name lookup,
  per-model entity/Pset/property and unit caches, and a spatial hash per level.
- Read resolved camera optics once and compare helper observations in numpy
  batches, retaining exact distance arithmetic, ownership precedence and ties.
- Add exclusive stage timings to CLI JSON and optional Python timing output;
  retain the seven semantic counters and deterministic kind-layer contents.
- Add a fresh-process benchmark with strict p95 enforcement and byte equality
  against the preceding importer, plus cell, unit, alias and shared-Pset tests.
- Keep schema metadata 0.2.1 and study algorithm revision 4.

## 0.4.6

- Read IFC aggregate/nest ancestry for helper ownership, ahead of
  SuperComponent and unique same-level proximity; reject ambiguous ownership.
- Recognize generic graphics helpers through complete optics and formula
  observations. Matching optics permit a bounded 1 m search for offset
  pictures; other helpers retain the 0.5 m limit.
- Consolidate identical legacy symbol type records onto deterministic shared
  catalogs, retaining occurrence identities, poses and optical overrides.
- Record type and helper mappings in kind-layer metadata without changing
  the importer counters; retain unresolved pictures with a reason.
- Verify the demo Revit export: 45 cameras, 45 sensors, 49 helpers folded,
  zero unmatched, three types and 45 derived sectors without skips.
- Add neutral IFC4X3 ownership/type regressions and an optional export claim.
  Schema metadata remains 0.2.1; algorithm revision remains 4.

## 0.4.5

- Accept core schema `>=0.8.1,<0.9` in source and generated plugin metadata.
- Pin reproducible builds to core v0.8.3; document compatibility separately
  from the exact source revision and refresh the neutral Nix lock.
- Test acceptance of core 0.8.1/0.8.3 and rejection of 0.8.0/0.9.0.
- Keep CCTV schema metadata at 0.2.1 and the algorithm revision at 4.

## 0.4.4

- Exclude target samples enclosed by opaque bodies before coverage evaluation,
  using majority parity on three axes with numpy and Embree.
- Add the default-true `excludeEnclosedSamples` study driver and derived
  `enclosedSamples` result count; report original and evaluated counts in JSON.
- Warn with `cctvTargetMostlyEnclosed` above 50%; fully enclosed targets have
  zero coverage evidence and require a redefined region.
- Preserve legacy evaluation with the toggle disabled; invalidate stale
  sample caches and retain phase, transparency and extent policies.
- Add `V-enclosed-samples`, lobby/yard and open-shell regressions; advance
  additive schema metadata to 0.2.1 and the algorithm revision to 4.

## 0.4.2

- Default study destinations to binary usdc, with explicit text selection and
  a `writeShells` driver; retain depth flags and numeric results without meshes.
- Batch shell/result specs and validate the exported bytes before atomic publication.
- Reuse gathered inputs with layer identity, dirtiness, session digests and selective
  USD change notices; preserve bytes and mtime for unchanged publications.
- Share the Embree depth BVH across frusta, vectorize sample/candidate filtering,
  and reuse triangulated shapes across translations with world-space validation.
- Record 30-trial measurements for both facility configurations and verify every
  cached publication's mtime/hash. Budgets remain unchanged.
- Document native BVH persistence as unsupported by the installed binding;
  cold measurements continue to include rebuilding the BVH.

## 0.4.1

- Measure complete study preparation, geometry hashing, triangulation, BVH build,
  depth casting, target rays and output publication with exclusive stage timings.
- Add a reproducible 30-trial facility benchmark for 32 fixed views and the 29
  generated cameras, each against all 41 doors; retain strict budget failures.
- Cache immutable world geometry, merged buffers and Embree BVHs by content;
  invalidate hash memoization on USD changes and registry/algorithm changes.
- Vectorize conservative gprim bounds culling and convex quad triangulation;
  retain topology refusals, hole semantics and the original triangle winding.
- Resolve finite target rays exactly with stable coincident-blocker ordering,
  and preserve off-frustum samples and cross-level findings in culling tests.
- Add three portable family checks and facility/lobby correctness comparisons.

## 0.4.0

- Protect all input-layer aliases; derive/study anonymously and publish atomically.
- Apply one phase policy to providers, targets, exclusions and obstacles.
- Require complete, finite, consistent target results; harden security grading.
- Add camera collections, study-specific guides and explicit Tier-A settings.
- Triangulate simple concave faces; refuse invalid topology with mesh attribution.
- Version complete input fingerprints and honor occurrence illumination overrides.
- Read IFC camera contract tiers A/B with correct units, warnings and source optics evidence.
- Add target grid spacing/pass fraction, extent sampling and exclusion view sites.
- Refuse nonrectilinear study providers.
- Add fourteen integrity scenarios and six further gate claims.
- Require core 0.8.0, use analytic roles, and remove local converter monkeypatches.
